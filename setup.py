# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

setup(
        name='scrapy-mongo',
        version="0.0.1",
        packages=find_packages('src'),
        package_dir={'': 'src'},
)
